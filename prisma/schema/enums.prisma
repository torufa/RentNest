enum UserRole {
    ADMIN
    LANDLORD
    TENANT
}

enum AccountStatus {
    ACTIVE
    BANNED
}

enum PropertyStatus {
    AVAILABLE
    UNAVAILABLE
    RENTED
}

enum ReviewsStatus {
    PUBLISHED
    HIDDEN
    DELETED
}

enum RentalRequestsStatus {
    PENDING
    APPROVED
    REJECTED
}

enum PaymentsStatus {
    PENDING
    COMPLETED
    FAILED
}
